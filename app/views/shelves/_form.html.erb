<%= form_with(model: shelf, local: true) do |form| %>
  <% if shelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shelf.errors.count, "error") %> prohibited this shelf from being saved:</h2>

      <ul>
      <% shelf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shop_id %>
    <%= form.select(:shop_id, options_for_select([[shop.name, shop.id ]])) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
